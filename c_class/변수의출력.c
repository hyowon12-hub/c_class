#include <stdio.h>
int main(void)
{
	int number1 = 1; //number1 이라는 것을 통해 1에 접근
	printf("%d \n",number1);
	int number2 = 2; //number2 이라는 것을 통해 2에 접근
	printf("나이 : %d \n번호 : %d",number2,number2);

	//2*3 = 6
	//조건 : 변수를 3개 선언
	//곱하기는 *을 사용한다.
	int num1, num2, result;
	num1 = 2;
	num2 = 3;
	result = num1 * num2;#include <stdio.h>
		int main(void)
	{
		int numvber1 = 1; //number1 이라는 것을 통해 1에 접근
		printf("%d"number1);
		printf("")
			return 0;

	}
	printf("%d * %d = %d",num1,num2, result);
	int num2 = 3;

	return 0;
}
#include <stdio.h>
int main(void)
{
	unsigned int max_number = 2200000000;
	printf("%u\n",max_number);
	//10 10.0(실수)
	//교과서 35쪽
	// 비트 : 2의 거듭재곱이다
	int num1 = 1;
	printf("%d\n", sizeof(num1));
	printf("%d\n", sizeof(100));
	printf("%d\n", sizeof(3.14f
	printf("%d\n", sizeof(float));
	printf("%d\n", sizeof(short));

	return 0;
}