#include <stdio.h>

struct student
{
	char name[20];
	int age;
	int grade;
};

int main(void)
{
	struct student students[2] = {
		{"모은지",19,3},
		{ "홍길동",21,100 }
	};
	for (int i = 0; i < 2; i++) {
		printf("이름 : %s, 나이 : %d, 학년 : %d", stucdents[1], name, )
	}
	return 0;
}